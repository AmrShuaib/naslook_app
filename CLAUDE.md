# ناس لايف (Naslife) — دليل العمل للجلسات

هذا الملف يُقرأ تلقائياً في كل جلسة. يشرح بنية المشروع، طريقة البناء والنشر والاختبار، القيود البيئية، والقرارات المتخذة، حتى تكمل أي جلسة جديدة من حيث توقفت السابقة. التقارير للمالك (عمرو حسن شعيب، شركة أريب الرقمية) تكون **بالعربية**؛ رسائل الالتزام (commit) بالإنجليزية.

## ما هو المشروع

منصة اجتماعية وتجارية محلية (جدة والدمام): خريطة بمنشورات، دوائر (مجتمعات وأعمال)، سوق مستخدمين بمحفظة داخلية، دردشة، عروض، مدونة، ولوحة إدارة على `/admin`. الموقع الحي: `https://naslife.app`.

- **التطبيق**: Flutter 3.47 ويب (Riverpod 2). المجلدات: `lib/pages` (الشاشات)، `lib/api` (العميل والنماذج)، `lib/state` (المزوّدون)، `lib/core` (الثيم والوسائط)، `lib/ui` (الودجات المشتركة).
- **الخادم**: إضافات Fastify مستقلة في `server/*.js` تُحمَّل بترتيب `server/register.txt`، على Postgres 16. كل إضافة تنشئ جداولها بنفسها (`CREATE TABLE IF NOT EXISTS` و`ALTER ... ADD COLUMN IF NOT EXISTS`) وتُغلَّف بـ try/catch فتُعطَّل عند الخطأ بدل إسقاط الخادم. تتشارك عبر `globalThis.naslife*` (المحفظة، الإشعارات، الإعدادات).
- **التوثيق**: `docs/*.md` ملف لكل ميزة (السوق، المنشئ، العروض، الإدارة، المدونة، البريد، أكواد الدردشة). `docs/reports/` تقارير وتصاميم.
- **الفحص الشامل (24 سبتمبر 2026)**: `docs/reports/naslife_audit_2026-09-24.md` (وPDF بجانبه): خريطة الخدمات، رحلات المستخدم، الأهلية، 262 مشكلة مؤكدة بالدليل `file:line`، وخارطة طريق في القسم 8. ابدأ منه عند اختيار المهمة التالية. تحويل تقرير Markdown إلى PDF: `tools/dev/md_report_pdf.py`.

## الفروع والنشر

- فرع التطوير: `claude/intelligent-hypatia-owb3fk` (الخادم يسحبه؛ اسم الفرع مكتوب في `/opt/naslife/ops/branch` على الخادم). لا تدفع إلى فرع آخر.
- فرع `webapp-build`: ملفات الويب المبنية فقط.
- **النشر التلقائي** (`server/autodeploy.sh` عبر مؤقّت systemd كل دقيقتين): يسحب فرع الكود، ينسخ `server/*.js`، يضيف الأسطر **الجديدة** من `server/register.txt`، يعيد التشغيل، يفحص الصحة، ويتراجع عند الفشل. ويسحب `webapp-build` عند تغيّرها. لذلك: دفع الكود يكفي لنشر الخادم، وإضافة جديدة تحتاج سطر تسجيل في `register.txt`.
- **نشر الويب**: `flutter build web --release --pwa-strategy=none -o build/web` ثم `tools/publish_bundle.sh "رسالة"`. تحقق: `curl -sS https://naslife.app/main.dart.js | md5sum` يطابق `build/web/main.dart.js`.
- `.github/workflows/deploy.yml` و`ios.yml` موجودان، لكن **مهام GitHub Actions لا تبدأ أصلاً منذ 16 سبتمبر 2026** (تفشل خلال ثوانٍ بلا سجلات، غالباً حصة الدقائق أو الفوترة في حساب GitHub). لذلك نشر الويب محلياً بالسكربت إلزامي، وبناء iOS عبر Codemagic (`codemagic.yaml`: بناء بلا توقيع الآن، وTestFlight بعد حساب أبل).
- Flutter SDK في الجلسة: `<scratchpad>/flutter/bin` (يضيع مع الحاوية؛ إن غاب نزّل `flutter_linux_3.47.3-stable.tar.xz` وفكّه هناك).
- الخادم `91.108.111.246` خلف Caddy. **لا يوجد SSH من الجلسة**؛ أي أمر على الخادم يشغّله المالك.
- **Caddy**: كتلة `naslife.app` (تحويل www، ضغط، ترويسة CSP، تمرير عكسي إلى Node على 127.0.0.1:4000) في `/etc/caddy/Caddyfile` بين علامتي `# --- naslife.app` و`# --- end naslife.app`، وبجانبها كتلة `areebd.sa` لموقع الشركة. إن فُقدت كتلة ناس لايف (TLS internal error وصفحة «Caddy works!») يشغّل المالك `server/restore-caddy.sh` عبر `systemd-run` كما في رأس الملف. حدث ذلك في 21 سبتمبر 2026 بعد تركيب موقع الشركة لأن الإعداد لم يكن محفوظاً في الملف. أوامر SSH من PowerShell تُكتب بعلامات اقتباس مفردة وبلا `$`.

## الاختبار

- `dart analyze` يجب أن يكون نظيفاً، و`flutter test` (نحو 230 اختباراً) يجب أن يمر قبل كل دفع. اختبارات الودجات تستخدم `MockClient` ومفاتيح `Key('...')` على العناصر.
- اختبارات الخادم: `server/test/run.sh harness_x.mjs` أو `all` (Postgres محلي: `pg_lsclusters | grep -q online || pg_ctlcluster 16 main start`؛ قاعدة `naslife_test`، المستخدم `postgres/pg`). الأكثر استخداماً: `harness_market_b.mjs` (السوق والمدفوعات)، `harness_posts.mjs`، `harness_admin.mjs` (فيه إخفاقات قديمة معروفة من حالة سابقة، ليست تراجعاً).
- الفحص الحي: `tools/verify_*.sh` مع `NASLIFE_TOKEN` (رمز جلسة حساب المؤسس jeddahh/SA9954961، يُرسل في الترويسة `x-token` فقط). **لا يُكتب الرمز في أي ملف.** حساب الهاتف الفعلي للمالك هو SA8370980. حساب المنصة SA0000000.
- Chromium داخل الجلسة لا يصل إلى naslife.app (وكيل الشبكة)، فالتحقق الحي بـ curl فقط ولقطات الشاشة تأتي من المالك.
- بدائل الاختبار في التطبيق: `pickImageOverride`، `pickVideoOverride`، `VoiceRecordSession.factoryOverride`، `VoicePlayer.factoryOverride`، `VideoView.factoryOverride`، `LiveCamera.factoryOverride`، `bakePhotoOverride`، `nativeShareOverride`، `MessageSound.supportedOverride`، `openAppSettingsOverride`، `tempDirOverride`، `nativeMobileOverride` (محاكاة الجوال؛ `flutter test` يعمل كسطح مكتب)، `DeviceLocation.override`/`openSettingsOverride`، `LegalLinks.openOverride`، `iosNativeOverride` (محاكاة iOS لإخفاء المال وبدء وضع الضيف).
- اختبارات الخادم بقاعدة مستقلة: `NASLIFE_TEST_DB=naslife_test_x server/test/run.sh all` (تتصادم الجلسات المتوازية على `naslife_test`). خمس حزم تفشل لأسباب بيئية قديمة: admin وadmin_bootstrap وchat_tools (ملفات تجهيز غير موجودة) وowner وshare.

## قيود بيئية مهمة

- لا تستخدم `pkill -f` (يقتل الجلسة). خوادم التطوير تُدار بـ `tools/dev/serve.sh`.
- Postgres المحلي قد يتوقف بين الأوامر؛ شغّله قبل الاختبارات.
- الملفات المؤقتة في مجلد الجلسة (scratchpad) تضيع مع الجلسة؛ كل ما يستحق البقاء يُوضع في `tools/` أو `server/test/` أو `docs/`.
- **Caddy يفرض CSP عاماً** يحلّ محل أي ترويسة CSP من التطبيق ويمنع السكربتات الخارجية في صفحاتنا (لذلك الدفع عبر صفحة ميسر المستضافة لا نموذج مضمّن). تعديل Caddy يحتاج المالك.

## القرارات المعمارية

- **المحفظة** دفتر داخلي (`wallet_accounts`/`wallet_tx` في `server/commerce.js`)؛ كل العمليات المتكررة (طلبات السوق، سبوت لايت، تحويلات) داخلية بلا رسوم بوابة.
- **الدفع** (`server/market_b.js`): بوابة ميسر. الشحن ينشئ **فاتورة مستضافة** لدى ميسر (`POST /pay/topup` يعيد `checkoutUrl` على نطاق ميسر بالعربية، `hosted:true`) مع `success_url`/`back_url` إلى `/pay/return?p=<id>`؛ التحقق من الخادم بالمفتاح السري، قيد مرة واحدة (idempotent)، ويبهوك يقرأ `secret_token` من الجسم. النموذج المضمّن `/pay/checkout/:id` يبقى خلف `PAY_EMBED=1`. مفاتيح ميسر تُدار من لوحة الإدارة (جدول `payment_settings`، السر لا يُعاد أبداً) مع فحص اتصال، ولها الأولوية على متغيرات البيئة. أبل باي عبر صفحة ميسر فقط. رد ميسر 405 = الحساب الحي غير مفعّل (`account-inactive`).
- **المنشئ (Composer v2)**: الكاميرا أولاً (ويب) مع كاميرا النظام احتياطاً، ثلاثة أنماط (صورة/فيديو/نص)، خمسة فلاتر تُخبز في الصورة قبل الرفع، الصوت طبقة على الصورة أو النص (`audio_url/audio_sec` في `map_posts`)، شاشة نشر واحدة إلى الخريطة أو دائرة أو السوق. تفاصيله في `docs/composer.md`.
- **السوق v2 والمرحلة ب**: سبوت لايت، بازارات، مندوب توصيل، ترقية البائع إلى دائرة، طبقة السوق على الخريطة. تفاصيله في `docs/market.md`.
- الإعدادات الحية للمنصة في `platform_settings` عبر `admin.js` وتُقرأ من `globalThis.naslifeSettings`.

## تطبيق iOS ومتجر أبل (سبتمبر 2026)

- مشروع `ios/`: المعرّف `app.naslife`، الاسم «ناس لايف»، آيفون فقط وعمودي، نصوص الأذونات بالعربية، `PrivacyInfo.xcprivacy`، دورة حياة المشاهد (SceneDelegate) كقالب Flutter 3.47، أيقونة الهوية (مصدرها `tools/brand/`).
- متطلبات المراجعة المنفّذة: حذف الحساب من التطبيق (`server/account_delete.js`، `docs/account-deletion.md`)، الخصوصية والشروط والدعم (`server/legal_pages.js`، `docs/legal-pages.md`) مع موافقة إلزامية عند التسجيل وورقة موافقة للحاليين.
- منفّذ أيضاً (24 سبتمبر 2026): التشغيل الأصلي (الكاميرا والصوت والفيديو والمشاركة والموقع مع جدة احتياطاً خارج منطقة الخدمة)، التصفح بلا حساب (`GuestShell` في `lib/app/app.dart`، افتراضي في iOS، و`requireAccount` في `lib/core/require_account.dart` قبل أي فعل يحتاج حساباً)، ورقة بلاغ موحّدة بأسباب جاهزة وحظر على كل محتوى (`lib/ui/report_sheet.dart`، ومنها الدائرة نفسها بنوع `vessel`)، وقائمة «بلاغات المحتوى» في لوحة الإدارة، وإخفاء المال في iOS (`lib/core/platform.dart`: الترويسة `x-naslife-client` من النسخة الأصلية فقط).
- دليل الرفع وملاحظات المراجِع وملصق الخصوصية: `docs/ios-release.md`. المؤجّل: إشعارات APNs (تحتاج مفتاح .p8 من حساب أبل).
- قاعدة أبل للمشتريات: «سبوت لايت» إعلان رقمي داخل التطبيق فلا يُباع في نسخة iOS (يُخفى، والخادم يرفض الطلب بترويسة `x-naslife-client: ios/...`)؛ السلع المادية والخدمات والتذاكر تبقى على المحفظة/ميسر.

## القرارات التنظيمية وحالة المدفوعات (سبتمبر 2026)

- محفظة قابلة للسحب إلى البنك مع تحويل بين المستخدمين = «إصدار نقود إلكترونية» عند ساما وتحتاج ترخيصاً أو العمل وكيلاً لمرخّص. قواعد ساما للتجارة الإلكترونية (2024) تمنع غير المرخّص من تسوية الأموال للبائعين. التقرير الكامل بالمصادر: `docs/reports/Naslife_Wallet_Payments_Report_2026-09-19.pdf`.
- **المسار الموصى به** (بانتظار قرار المالك): الدفع لكل طلب في السوق عبر بوابة مرخّصة تقسّم للبائع (Tap Marketplace أو ميسر Platforms)، وإبقاء المحفظة **مغلقة** للإنفاق الداخلي بلا سحب ولا تحويل بين المستخدمين، ورفع الحد الأدنى للشحن إلى 100 ريال.
- **مؤجّل**: ميزة سحب أرصدة البائعين إلى البنك (كانت المهمة #79) لأنها النشاط المحظور على غير المرخّص.
- **قبل أي مدفوعات حقيقية**: إطفاء «الشحن التجريبي» (`testTopup`) من إعدادات الإدارة، وخفض `maxTopup`. حساب ميسر الحي (Areeb Digital) بانتظار تفعيل المنشأة؛ حتى ذلك تُستخدم مفاتيح الاختبار.
- ضريبياً: من 2026 قد تُعامَل المنصة كمورّد عن البائعين غير المسجّلين (المادة 47/3)؛ يحتاج محاسباً قبل الإطلاق.

## الشركة

- الشركة: أريب الرقمية (Areeb Digital)، النطاق `areebd.sa`. موقع الشركة في المستودع المستقل `AmrShuaib/areebdigital` (فرع `main`، جلسة مستقلة تبنيه) بنفس نمط النشر (Caddy + مؤقّت سحب `areeb-pull.timer` إلى `/opt/areeb/site`) على الخادم نفسه؛ البريد عبر Google Workspace.
- حسابات المتاجر (Apple Developer وGoogle Play) ستُفتح باسم الشركة وتحتاج رقم D-U-N-S وموقعاً باسم الشركة وصفحتي خصوصية ودعم.

## اصطلاحات

- نصوص الواجهة عربية؛ التعليقات في الكود عربية قصيرة تشرح «لماذا».
- كل ميزة جديدة: إضافة خادم مستقلة + سطر في `register.txt` + نماذج وواجهة في `lib/api` + شاشة + اختبار ودجات + حزمة خادم + ملف في `docs/` + تقرير عربي للمالك بعد التحقق الحي.
- لا أسرار في المستودع (رموز الجلسات، مفاتيح ميسر، مفاتيح الويبهوك). المفاتيح الحية تُدخل من لوحة الإدارة.
- لا تُنشئ pull request إلا بطلب صريح.
